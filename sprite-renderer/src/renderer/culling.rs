//! Frustum culling for off-screen sprite optimization

/// Frustum culling system for performance optimization
pub struct FrustumCuller {
    // Placeholder - will be implemented in future stories
}

impl FrustumCuller {
    /// Create a new frustum culler
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for FrustumCuller {
    fn default() -> Self {
        Self::new()
    }
}
// Development Console - Standalone Application
// This module provides a standalone console application for testing and development

use dev_console::{ConsoleCommand, ConsoleCommandResult, ConsoleCommandRegistry, ConsoleOutput, DevConsole};

fn main() {
    println!("Dev Console - Standalone Application");
    println!("This is a library crate, not meant to be run directly.");
} 